@model List<CapaEntidad.entEspecialidad>

<div class="card p-3">
    <h3>Registrar Especialidad</h3>
    <form asp-action="Insertar" method="post">
        <div class="row mb-3">
            <div class="col">
                <input type="text" name="nombre" class="form-control" placeholder="Nombre de especialidad" required />
            </div>
            <div class="col-auto">
                <button type="submit" class="btn btn-success">Registrar</button>
            </div>
        </div>
    </form>

    <h4>Lista de Especialidades</h4>
    <table class="table table-bordered">
        <thead>
            <tr><th>Nombre</th><th>Acciones</th></tr>
        </thead>
        <tbody>
            @foreach (var esp in Model)
            {
                <tr>
                    <td>@esp.nombre</td>
                    <td>
                        <button class="btn btn-sm btn-danger" onclick="eliminar(@esp.idEspecialidad)">Eliminar</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <script>
        function eliminar(id) {
            if (confirm("¿Eliminar especialidad?")) {
                $.post('/MantenedorEspecialidad/Eliminar', { id: id }, function () {
                    cargarVistaParcial('/MantenedorEspecialidad/Index');
                });
            }
        }
    </script>
}
